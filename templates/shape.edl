{% extends 'widget.edl' %}
{% block properties %}
{% if widget.fill %}fill
fillColor {{widget.fill}}{% endif %}
lineWidth {{widget.lineWidth}}
lineColor index {{widget.lineColor.value}}
{% endblock %}
